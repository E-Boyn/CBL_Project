/** Manages interaction between the Enemy and Dagger cards. 
 * Tracks whether either card is focused.
 */
public class EnemyAndDagger {
    boolean enemyFocused;
    boolean daggerFocused;

    Enemy enemy;
    Dagger dagger;
    
    public EnemyAndDagger() {
    }
}